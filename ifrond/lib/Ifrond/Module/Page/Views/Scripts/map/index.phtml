<h1>Разделы сайта</h1>
<div class="typography">
<ul id="mapTree">
<?php
function parseTree($branch) {
	
	foreach ($branch->children() as $t) {
   		echo '<li><a href="/page/index/permlink/id/'.$t['id'].'/">'.$t['title'].'</a>';
   		if (sizeof($t->children()) > 0) {
   			echo '<ul>';
   			parseTree($t);
   			echo '</ul>';
   		}
   		echo '</li>';
	}
	
}
parseTree($this->tree);
?>
</ul>
</div>