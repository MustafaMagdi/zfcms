<div id="parentsPath"><a href="/">Главная страница</a> / 
<?php
$url = $this->baseURL;
$parents = array();
foreach ($this->parents as $t) {
    $url = dirname($url);
    $parents[] = '<a href="' . $url . '/">' . $t->title . '</a>';
}
krsort($parents);
echo implode($parents, ' / ');
?>
</div>